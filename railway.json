{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "echo 'skipping the react-router build - not needed for the duel server'"
  },
  "deploy": {
    "startCommand": "npm run duel-server",
    "restartPolicyType": "ON_FAILURE"
  }
}
